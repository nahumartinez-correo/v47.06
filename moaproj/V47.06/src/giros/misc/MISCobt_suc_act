/****o* Pendientes de agrupación/OFB:giros:MISCobt_suc_act
* NOMBRE
*   OFB:giros:MISCobt_suc_act
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "girosmisc.h"

SUB giros:MISCobt_suc_act
    /*Obtengo la sucursal activa para poder recuperar las caídas en vuelo*/
    CLEARFIELD common:sucursal_activa 
    CALL siaf:CDSadt_sucur_load
    lib:err_retval = RETVAL
    IF (RETVAL < 0) THEN
        RETURN (-2)
    ELSE 
        common:sucursal_activa = siaf:adt_sucur_codigo
    END IF 
    RETURN (lib:err_retval)
END SUB 

/*******/
