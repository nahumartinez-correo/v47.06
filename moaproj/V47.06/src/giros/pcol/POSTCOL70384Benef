/****o* Pendientes de agrupación/OFB:giros:POSTCOL70384Benef
* NOMBRE
*   OFB:giros:POSTCOL70384Benef
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "girospcol.h"

sub giros:POSTCOL70384Benef
/*	if (OcupacionBenef_fld == "** OTRA ACTIVIDAD **") then
    	if (OtrosOcupBenef_fld.numchars == 0) then
        	ERRMSG LST:common:errmsg_list,gali:ERRcampo_requerido
            NEXTFIELD OtrosOcupBenef_fld
            return (-2)
        end if
    end if
	IF (!TESTFLAG(CuitInter_flag,NoCuitBenef_inter) && (NroCuitBenef_fld.numchars == 0)) then
        	ERRMSG LST:common:errmsg_list,"FALTA INGRESAR EL NRO. CUIT/CUIL/CDI"
            NEXTFIELD NroCuitBenef_fld
            return (-2)
    end if*/
end sub

/*******/
