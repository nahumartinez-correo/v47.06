/****o* Pendientes de agrupación/OFB:giros:CDSEstados_load
* NOMBRE
*   OFB:giros:CDSEstados_load
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "giroscds.h"

sub giros:CDSEstados_load
	CDS SELECT FROM DSC:Estados_tab_dsc BECOMES DSC:tipo_giro_ctx_dsc WHERE  \
	adt_est_cod == FLD:CodiEstado_fld 
    	CDS FINDFIRST DSC:tipo_giro_ctx_dsc LOCK 	
    	let lib:err_retval = cdsretval
    	if (lib:err_retval == -801) then
    		return (-2)
    	end if
    	cds extract DSC:tipo_giro_ctx_dsc all
    	CodiEstado_fld = adt_est_cod
    	DescriEstado_fld = adt_est_desc
    	cds unlock DSC:tipo_giro_ctx_dsc
    	cds endselect DSC:tipo_giro_ctx_dsc
end sub

/*******/
