/****o* Pendientes de agrupación/OFB:giros:CDSmaxItemGiroChq
* NOMBRE
*   OFB:giros:CDSmaxItemGiroChq
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "giroscds.h"

SUB giros:CDSmaxItemGiroChq()

   /********************************************************************************************************/
   /*                                                                                                      */
   /* [PAW - 06/02/99]      Esta rutina se encarga de devolver, según el operador activo, el máximo        */
   /*                       número de ítem que tiene asociado.                                             */
   /*                                                                                                      */
   /* Salidas:                                                                                             */
   /* - adt_girChqtmp_sec:   Devuelve el máximo número o 0 en caso de no existir números relacionados.     */
   /*      (giros)                                                                                         */
   /*                                                                                                      */
   /********************************************************************************************************/

	CDS SELECT FROM DSC:girosChqTmpTAB BECOMES DSC:girosChqTmpCON \
		WHERE adt_girChqtmp_op == op:op_operator_id && adt_girChqtmp_rem == 0
	CDS FINDFIRST DSC:girosChqTmpCON LOCK
	if (cdsretval != 0)
		adt_girChqtmp_sec = 0
	else
		CDS AGGREGATE DSC:girosChqTmpCON max adt_girChqtmp_sec
		CDS EXTRACT DSC:girosChqTmpCON adt_girChqtmp_sec adt_girChqtmp_op
	endif
	CDS UNLOCK DSC:girosChqTmpCON
END SUB

/*******/
