/****o* Pendientes de agrupación/OFB:post:TRANdetSurPostal
* NOMBRE
*   OFB:post:TRANdetSurPostal
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "posttran.h"

SUB post:TRANdetSurPostal
    /* Expedicion Sur Postal             */
    /* Sept. 1998 - B. Petrella -            */

	if (orden >= orden_max)	/* Termino de procesar */
		let drv:INDrun_funct = LST:post:TRANcteLinSurPostal
		let orden = 0
	else
		let drv:INDrun_funct = LST:post:TRANdetSurPostal
	end if
END SUB

/*******/
