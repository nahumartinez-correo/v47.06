/****o* Pendientes de agrupación/OFB:post:PRES99480
* NOMBRE
*   OFB:post:PRES99480
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postpres.h"

SUB post:PRES99480

	siaf:codigo_siaf =  99480
	siaf:moneda = "99"
	
	
	//Recupero datos de la presu
    //call post:MISCValidoAdelanto
  	
  	IF (post:estado_presu != 2 && post:estado_presu != 3 && post:estado_presu != 4)
	    	errmsg common:errmsg_list,"Inicie una Sesión con Cliente Medio para realizar la Facturación de los Intereses."
		    return (-2)
    END IF
    
	if (post:estado_presu != 4) then
	   	
	   	if (post:consulta_excepcionCM == "S")
	    	errmsg common:errmsg_list,"Ud.se encuentra en una venta por excepción.\nNo podrá realizar la Facturación de Intereses."
	    	return (-2)
		endif 
	
	   	if (post:consulta_ordenCM == "S")
	    	errmsg common:errmsg_list,"Ud.se encuentra en una venta por Orden de Compra.\nNo podrá realizar la Facturación de Intereses."
	    	return (-2)
		endif
				    	
		IF (post:cli_tipo != CLIMED)
		    errmsg common:errmsg_list,"El cliente no está habilitado para realizar una Facturacion de Intereses."
		    return (-2)
	    END IF
	    
	    IF (ExcepSinLinea) THEN
		    errmsg common:errmsg_list,"La sesión se ha iniciado fuera de línea.\nPara realizar la Facturacion deberá iniciar la sesión nuevamente."
		    return (-2)
	    END IF
    
    	IF (post:estado_presu == 2) THEN
	    	CALL CDSBusca_TxPresuDet
		    if (retval < 0) then
		    	errmsg common:errmsg_list,"La transacción de Facturación de Intereses debe ser la única del Presupuesto."
	    		return (-2)
	    	end if
		END IF
		
  	end if
  	
  	Call siaf:PRESfecha_hora
  	If ( Retval < 0 )
      	Return -1
    Endif
    

END SUB

/*******/
