/****o* Pago en Destino/OFB:post:RCVEcons_PEDpend
* NOMBRE
*   OFB:post:RCVEcons_PEDpend
* DESCRIPCION
*   Rutina que recibe los datos correspondientes al mensaje de consulta de Parametría CCT
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*  OFB:post:OFBcons_PEDpend
* DOCUMENTACION
*   http://jira.sibs.net/jira/browse/AUT-1684 
* SOURCE
*/
#include "postrcve.h"

SUB post:RCVEcons_PEDpend

	//Usa autorizador de Clientes Medios por lo que uso el hdr de clientes medios
	call post:RCVEhdr_climedio
	if (hcomm:tran_status == HOST_ACCEPTED )
		call post:RCVEgral_climedio
		if ( retval < 0 )
			return -2
		end if
	else
		return -2
	end if

	call post:MISCconsMult_PEDpend
	if (retval < 0 )
		return -2
	end if
	
END SUB

/*******/
