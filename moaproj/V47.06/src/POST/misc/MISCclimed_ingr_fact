/****o* Pendientes de agrupación/OFB:post:MISCclimed_ingr_fact
* NOMBRE
*   OFB:post:MISCclimed_ingr_fact
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postmisc.h"

SUB post:MISCclimed_ingr_fact(varl_codigo)

	local varl_esta like giros:EstadoGiro_fld

	call post:MISCcm_trx_datos(varl_codigo)
	if (retval < 0 )
		return -2
	end if

	call post:MISCcm_armar_remitos
	if (retval < 0 )
		return -2
	end if

	call post:MISCcm_armar_facts
	if (retval < 0 )
		return -2
	end if

	if (post:OperacionCob)
		varl_esta = "C"
	else
		varl_esta = "P"
	end if

	call post:MISCcm_armar_factnva(varl_esta)
	if (retval < 0 )
		return -2
	end if

	call post:MISCcm_armar_adel
	if ( retval < 0 )
		return -2
	end if

END SUB

/*******/
