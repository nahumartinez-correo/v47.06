/****o* Pendientes de agrupación/OFB:post:MISCconsultoAbonado
* NOMBRE
*   OFB:post:MISCconsultoAbonado
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postmisc.h"

/* Me fijo si en los remitos hay algún Ingreso o Egreso para preguntar si abona o no
	el impuesto */
SUB post:MISCconsultoAbonado
	Cds select from dsc:TABrem_item becomes dsc:CONrem_item where \
	post:op_operator_id == op:op_operator_id && \
	(post:item_codigo_trx == 6800 || post:item_codigo_trx == 6600)
	cds findfirst dsc:CONrem_item nolock
	/* Si tiene algún Ingreso o Egreso me fijo si se abono o no el impuesto */
	if (cdsretval == 0) then
		call giros:MISCImpAbonadoCheq
	end if
	cds endselect dsc:CONrem_item
END SUB

/*******/
