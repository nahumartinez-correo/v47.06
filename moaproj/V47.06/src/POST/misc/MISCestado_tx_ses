/****o* Pendientes de agrupación/OFB:post:MISCestado_tx_ses
* NOMBRE
*   OFB:post:MISCestado_tx_ses
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postmisc.h"

SUB post:MISCestado_tx_ses(pos)
/*Esta rutina verifica el estado de la tx que compone una sesion.
 Los pueden ser MARCADA(0) o DESMARCADA(-1)*/
	select (pos)
	case 0
		if (testflag(est_ses_check,item_0))
			return (0)
		else
			return (-1)
		endif
	case 1
		if (testflag(est_ses_check,item_1))
			return (0)
		else
			return (-1)
		endif
	case 2
		if (testflag(est_ses_check,item_2))
			return (0)
		else
			return (-1)
		endif
	case 3
		if (testflag(est_ses_check,item_3))
			return (0)
		else
			return (-1)
		endif
	case 4
		if (testflag(est_ses_check,item_4))
			return (0)
		else
			return (-1)
		endif
	case 5
		if (testflag(est_ses_check,item_5))
			return (0)
		else
			return (-1)
		endif
	case 6
		if (testflag(est_ses_check,item_6))
			return (0)
		else
			return (-1)
		endif
	case 7
		if (testflag(est_ses_check,item_7))
			return (0)
		else
			return (-1)
		endif
	case 8
		if (testflag(est_ses_check,item_8))
			return (0)
		else
			return (-1)
		endif
	case 9
		if (testflag(est_ses_check,item_9))
			return (0)
		else
			return (-1)
		endif
	end select
END SUB

/*******/
