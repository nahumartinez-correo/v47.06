/****o* Pendientes de agrupación/OFB:post:MISCinic_AuxImpp
* NOMBRE
*   OFB:post:MISCinic_AuxImpp
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postmisc.h"

SUB post:MISCinic_AuxImpp
/*Agrego que seleccione sólo aquellos registros que que tengan importe mayor a 0
  Gabriel Falciola*/
        Cds SELECT FROM Dsc:AuxImpp_tbl BECOMES DSC:AuxImpp_ctx  \
                WHERE (Fld:adt_auximpp_op == fld:post:op_operator_id && \
                PresNro == fld:PresNro && adt_auximpp_importe > 0)
        Let lib:err_retval = cdsretval
END SUB

/*******/
