/****o* Pendientes de agrupación/OFB:post:MISCSumaRest
* NOMBRE
*   OFB:post:MISCSumaRest
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postmisc.h"

SUB post:MISCSumaRest(TipoRest,AdmiteCF,LocErr)
	
	if (post:adt_rmedpag_cfiscal == "S") then
        /* Admite CF */
        if (AdmiteCF == 0) then
    		/*Si admite CF y tiene restriccion por inclusión 
    		admite igual el medio de Pago (Esté o no incluida)*/
    		if (TipoRest == "I" ) then
    			return (0)
    		else
    			/* Admite CF y tiene restriccion por exclusion y NO 
    			está excluída, admite el Medio de Pago */
    			if (TipoRest == "E" && LocErr == 0) then
    				return (0)
    			else
    				/* Tiene rest "E" y está excluida */
    				return (-1)
    			end if
    		end if				        		
        else
	        /* No Admite CF */
	        /* Si no admite CF, pero tiene restriccion por "I" y está
	        incluída, admite igual el medio de Pago */
	        if (TipoRest == "I" && LocErr == 0) then
	        	return (0)
	        else
	        	/*Para el resto, no lo admite*/
	        	return (-1)
	        end if					        
	    end if    
	else
	/* No tiene restricciones por CF, vale la rest por transaccción */
		return (LocErr)								        
	end if
END SUB

/*******/
