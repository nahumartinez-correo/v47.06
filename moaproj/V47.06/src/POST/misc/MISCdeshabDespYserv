/****o* Pendientes de agrupación/OFB:post:MISCdeshabDespYserv
* NOMBRE
*   OFB:post:MISCdeshabDespYserv
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postmisc.h"

SUB post:MISCdeshabDespYserv
    /* Despachos y Servicios */
    /* Agos. 1998 - B. Petrella */
      let drv:button = 2 /* Combo asoc al cpo. Apertura */
    call drv:but_disable
      let drv:button = 3 /* Combo asoc. al cpo. clase de cont. */
    call drv:but_disable
      let drv:button = 4 /* Combo asoc al cpo. Tipo de Serv. */
    call drv:but_disable
END SUB

/*******/
