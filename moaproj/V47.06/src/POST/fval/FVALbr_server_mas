/****o* Pendientes de agrupación/OFB:post:FVALbr_server_mas
* NOMBRE
*   OFB:post:FVALbr_server_mas
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postfval.h"

SUB post:FVALbr_server_mas(p_campo)
	common:csr_string = p_campo
	IF (common:csr_string.NUMCHARS == 0 ) THEN
		FPRINT common:csr_string "El server no puede ser nulo"
		RETURN -2
	END IF
	call post:FVALcadenaVal_mas(common:csr_string)
    IF (RETVAL == -2)
        FPRINT common:csr_string "El server posee un caracter inválido"
		RETURN -2
	END IF
END SUB

/*******/
