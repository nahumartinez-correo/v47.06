/****o* Pendientes de agrupación/OFB:post:FVALformu_codseqB
* NOMBRE
*   OFB:post:FVALformu_codseqB
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postfval.h"

SUB post:FVALformu_codseqB
    IF (adt_formu_codseqB.numchars == 0) THEN
        adt_formu_codseqB = " "
        refresh adt_formu_codseqB
    END IF
    fld:adt_secuen_sucnro = fld:common:sucursal_activa
    fld:adt_secuen_cod = fld:adt_formu_codseqB
    CALL CDSsecuen_load
    IF (RETVAL != 0) THEN
        drv:button = 6
        call drv:but_disable
        call CDSsecuen_sel
        fld:adt_formu_codseqB = fld:adt_secuen_cod
        refresh adt_formu_codseqB
    END IF
    setvaldone adt_formu_codseqB
    drv:button = 6
    call drv:but_enable
END SUB

/*******/
