/****o* ABM´s/OFB:post:FVALcloApe_ingbr
* NOMBRE
*   OFB:post:FVALcloApe_ingbr
* DESCRIPCION
*   Rutina de validacion del campo Apellido del abm de Ingresos Brutos
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   SCR:post:SCRNingbr
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postfval.h"
SUB post:FVALcloApe_ingbr
    IF (TESTFLAG(admin:mode_flags,imprimir)) THEN
      SETVALDONE CloCuit
      SETVALDONE CliRaz
      SETVALDONE CloNombre
      SETVALDONE CloApellido
      SETVALDONE IvaCod
      SETVALDONE adt_TipoDoc_tipo
      SETVALDONE adt_prvta_pcia_cod
      SETVALDONE IngBCod
	  SETVALDONE CloExento
      UNPROTFIELD CloCuit
      RETURN (0)
	END IF
	if (post:adt_TipoDoc_tipo.numchars == 0)
		nextfield post:adt_TipoDoc_tipo
	end if
	if (IvaCod.numchars == 0 ) then
	    if (CloTipDoc.numchars == 0 ) then
	        return 0
	    end if
	end if
	CALL siaf:FVALcampo_requerido
    IF (RETVAL < 0) THEN
        NEXTFIELD CloApellido
        RETURN (-2)
    END IF
END SUB

/*******/
