/****o* Pendientes de agrupación/OFB:post:FVALvd_cp
* NOMBRE
*   OFB:post:FVALvd_cp
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postfval.h"

SUB post:FVALvd_cp
    /* COMENTO LA FVAL HASTA QUE SE CARGUE CORRECTAMENTE LA
       TABLA QUE CONTIENE LOS CODIGOS POSTALES
    IF (post:cp != "") THEN
        CDS SELECT FROM CODPOST_TAB BECOMES CODPOST_CTX WHERE \
            post:adt_codpost_cp == post:cp
        CDS FINDFIRST CODPOST_CTX NOLOCK
        IF (CDSRETVAL != 0) THEN
            MSGBOX "CODIGO POSTAL INEXISTENTE",0x00,"ERROR"
            CDS ENDSELECT CODPOST_CTX
            RETURN (-2)
        ELSE
            CDS ENDSELECT CODPOST_CTX
            RETURN (0)
        END IF
    ELSE
        MSGBOX "DEBE INGRESAR UN CODIGO POSTAL",0x00,"ERROR"
        RETURN (-2)
    END IF */
END SUB

/*******/
