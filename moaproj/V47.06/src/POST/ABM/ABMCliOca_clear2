/****o* Pendientes de agrupación/OFB:post:ABMCliOca_clear2
* NOMBRE
*   OFB:post:ABMCliOca_clear2
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postABM.h"

SUB post:ABMCliOca_clear2

    CLEARFIELD CloRazon
    CLEARFIELD IvaCod
    CLEARFIELD CloInscripto
    CLEARFIELD CloSucAlt
    CLEARFIELD CloCuit
    CLEARFIELD CloFecAct
    CLEARFIELD CloCalle
    CLEARFIELD CloNroDir
    CLEARFIELD CloCodPost
    CLEARFIELD CloLoc
    CLEARFIELD CloPartido
    CLEARFIELD CloProvin
    CLEARFIELD CloPiso
    CLEARFIELD CloTelefono
    CLEARFIELD adt_pcia_desc
    CLEARFIELD CloCatCodIB
    CLEARFIELD CloNroInb
	CLEARFIELD CatGCod
	CLEARFIELD CatECod
END SUB

/*******/
