/****o* Pendientes de agrupación/OFB:post:PRTcortePagina
* NOMBRE
*   OFB:post:PRTcortePagina
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postprt.h"

SUB post:PRTcortePagina(INDICE, SUBTOTAL, LARGO, Tipo_Impresion)
	
	
	/*if (Tipo_Impresion == 3 )
		// Ticket no es Necesario un salto de Pagina
		return (INDICE)
	endif
	If ( INDICE >= LARGO)
		if (LARGO == PAGE_FACTURA_CUERPO)
			Call post:MISCavaz_linea_imp(2)
			INDICE = INDICE + 2
		endif
		Fprint common:csr_string "%78.78s %9.9e\n", "TRANSPORTE ", SUBTOTAL
		call post:MISCprt2laser(common:csr_string)
		INDICE = INDICE + 1
			Call PRT_CAI(INDICE)
		call post:MISCprt2laser(SALTODEPAGINA)
		Call post:MISCseteo_documento
		Call post:PRT_Encabe_Fac

		Fprint common:csr_string "%-78.78s %9.9e\n",\
				"TRANSPORTE ", SUBTOTAL
		call post:MISCprt2laser(common:csr_string)
		INDICE = 1
	Endif*/
	
	return (INDICE)
END SUB


/*******/
