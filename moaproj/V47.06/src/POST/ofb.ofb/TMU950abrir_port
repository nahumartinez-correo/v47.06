/****o* Pendientes de agrupación/OFB:post:TMU950abrir_port
* NOMBRE
*   OFB:post:TMU950abrir_port
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postofb.ofb.h"

SUB post:TMU950abrir_port
    	LOCAL pto_serie LIKE common:csr_string
    	local port like common:csr_string   /*gali:port */
    	local open_flag like gali:open_flag
	/* Anulo la tecla ESC porque esto trae problema si se presiona 
           	     mientras opero con el puerto */
	KEYDEF push
	STACK common:trap_reset PUSH
	KEYDEF clear
	LET common:trap_reset = OFB:plataf:KEYSanulo_esc
	/* dice con que puerto trabajo (de acuerdo al descriptor) */
	call csr:get_profile_string("project","puerto_TMU950",pto_serie)
	if (retval != 0)
		msgbox "No está configurado el puerto serial",OFB_OK,"Aviso"	
		return -1
	end if
	FPRINT port "//%s/%s" common:window_node pto_serie
	LET open_flag = 2
	CALL CSR:wingsp GSP_OPEN port open_flag fd
	RETURN (RETVAL)
END SUB

/*******/
