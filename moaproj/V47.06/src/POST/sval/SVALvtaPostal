/****o* Pendientes de agrupación/OFB:post:SVALvtaPostal
* NOMBRE
*   OFB:post:SVALvtaPostal
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postsval.h"

SUB post:SVALvtaPostal
	IF (TESTFLAG(flag_vtaPostal,en_tranGral))
		LET drv:INDrun_funct = lst:post:TRANgral
    	CALL drv:do_mode
    ELSE
    	/*MSGBOX "Desea salir de la Transacción de Ventas Postales",OFB_YESNO,"Atención"
        IF (RETVAL == 0X07) THEN //NO
            RETURN -1
        END IF*/
    END IF
  //  setflag drv:mode_flags,no_stack_list
END SUB

/*******/
