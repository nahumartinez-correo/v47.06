/****o* Pendientes de agrupación/OFB:post:SVALcontXsaca
* NOMBRE
*   OFB:post:SVALcontXsaca
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postsval.h"

SUB post:SVALcontXsaca
    /* Corte Operativo - Llenado de Saca.   */
    /* B. Petrella - May. 1998              */
    /*let siaf:afecta_efectivo = " "
    CALL siaf:SVALnivel_sup
    IF (RETVAL < 0) THEN
        RETURN (-2)
    END IF*/
    /* Esta es la pantalla que journaliza el detalle */
    /* de cada pieza que contiene la saca.           */

    drv:INDscr = SCR:post:SCRNllenSacaDet

END SUB

/*******/
