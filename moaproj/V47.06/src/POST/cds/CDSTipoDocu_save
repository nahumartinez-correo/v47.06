/****o* Pendientes de agrupación/OFB:post:CDSTipoDocu_save
* NOMBRE
*   OFB:post:CDSTipoDocu_save
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postcds.h"

SUB post:CDSTipoDocu_save
    CDS SELECT FROM DSC:TABTipoDoc BECOMES DSC:CONTipoDoc \
        WHERE adt_TipoDoc_tipo == FLD:adt_TipoDoc_tipo
    CDS FINDFIRST DSC:CONTipoDoc LOCK
    LET lib:err_retval = CDSRETVAL
    CDS CLEARBUFFER DSC:CONTipoDoc
    CDS APPEND DSC:CONTipoDoc FLD:adt_TipoDoc_tipo \
                                        FLD:adt_TipoDoc_desc \
                                        FLD:adt_TipoDoc_digver \
                                        FLD:adt_TipoDoc_ver2dig \
                                        FLD:adt_TipoDoc_alfanum \
                                        FLD:adt_TipoDoc_poliza \
                                        FLD:adt_TipoDoc_LGiro

    IF (lib:err_retval == -801) THEN
        CDS INSERT DSC:CONTipoDoc LOCK
    ELSE
      CDS REPLACE DSC:CONTipoDoc LOCK
    END IF
    CDS UNLOCK DSC:CONTipoDoc
END SUB

/*******/
