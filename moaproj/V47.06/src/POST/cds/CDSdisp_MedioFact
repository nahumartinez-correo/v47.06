/****o* Pendientes de agrupación/OFB:post:CDSdisp_MedioFact
* NOMBRE
*   OFB:post:CDSdisp_MedioFact
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postcds.h"

Sub post:CDSdisp_MedioFact
	CDS SELECT FROM DSC:Monedas_tbl BECOMES DSC:Monedas_ctx WHERE post:adt_mon_comp == "N" 
    CDS MULTIDISPLAY DSC:Monedas_ctx  post:LISTadt_moneda
    LET lib:err_retval = CDSRETVAL
    /* Salio con Esc */
    IF (lib:err_retval == 0) THEN
      RETURN (-2)
    END IF
    LET lib:err_retval = CDSRETVAL
    CALL lib:MISCcds_find_err
    IF (RETVAL != 0) THEN
      RETURN (-1)
    END IF
    CDS EXTRACT DSC:Monedas_ctx All
    RETURN (0)
End Sub

/*******/
