/****o* Pendientes de agrupación/OFB:post:CDSadtServLoadSinGir
* NOMBRE
*   OFB:post:CDSadtServLoadSinGir
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postcds.h"

SUB post:CDSadtServLoadSinGir
    CDS SELECT FROM DSC:adt_serv_table BECOMES DSC:adt_serv_context \
        WHERE adt_serv_serv == FLD:adt_serv_serv && (adt_serv_tipo != "MO" || adt_serv_serv == "18501")
    CDS FINDFIRST DSC:adt_serv_context LOCK
    LET lib:err_retval = CDSRETVAL
    IF (lib:err_retval == -801) THEN
      RETURN (-2)
    END IF
    CDS EXTRACT DSC:adt_serv_context ALL
    CDS UNLOCK DSC:adt_serv_context
    CALL ABMadt_serv_refresh
END SUB

/*******/
