/****o* Pendientes de agrupación/OFB:post:CDShay_egresos
* NOMBRE
*   OFB:post:CDShay_egresos
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postcds.h"

Sub post:CDShay_egresos(Codigo)
	Cds Select FROM DSC:RProduc_tbl BECOMES DSC:RProduc_ctx  where adt_rprod_mpa_cod == FLD:Codigo && ( post:adt_rprod_trn == 6600 || post:adt_rprod_trn == 6610 )
	Cds FindFirst DSC:RProduc_ctx nolock
	lib:err_retval = cdsretval
	If (( post:adt_rmedpag_tran == "S" && adt_rmedpag_ttipo == "E" && lib:err_retval == 0 )  )
		return 0
	End If
	If ( ( post:adt_rmedpag_tran == "S" && adt_rmedpag_ttipo == "I" && lib:err_retval != 0  ))
		; Esta última pregunta se hace por que podría no estar incluida la transacción en
		; la tabla, pero en caso de que tenga restricción por comprobante fiscal y tenga
		; comprobante fiscal, la debería tomar igual.
		if (adt_rmedpag_cfiscal != "S")
			return 0
		endif
	End If
	return -2
End Sub

/*******/
