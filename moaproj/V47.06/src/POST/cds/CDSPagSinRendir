/****o* Pendientes de agrupación/OFB:post:CDSPagSinRendir
* NOMBRE
*   OFB:post:CDSPagSinRendir
* DESCRIPCION
*   Carga en un picklist dinámico el número de documento y la razón social de los Clientes Mayoristas Crem
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   post:PBclimay_razon
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postcds.h"

SUB post:CDSPagSinRendir
    
    //VERIFICO SI EXISTEN OPERADORES PAGADORES SIN RENDIR
    
    CDS SELECT FROM "Beneficios" BECOMES "Beneficios_ctx" \
	WHERE \
	adt_benef_estado == "E" && \
	adt_benef_fecha_pag != common:system_date

	CDS FINDFIRST "Beneficios_ctx" nolock
    IF (CDSRETVAL == 0)
    	CDS ENDSELECT "Beneficios_ctx"
		RETURN (-2)
	END IF
	
	//SI LLEGO HASTA ACÁ ES QUE NO EXISTE NINGUN
	//OPERADOR PAGADOR SIN RENDIR
	CDS ENDSELECT "Beneficios_ctx"
	RETURN 0
	
END SUB

/*******/
