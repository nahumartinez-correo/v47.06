/****o* Pendientes de agrupación/OFB:post:PBescRtnRecepSaca
* NOMBRE
*   OFB:post:PBescRtnRecepSaca
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "postpb.h"

SUB post:PBescRtnRecepSaca
    /* RECEPCION DE SACA            */
    /* B. Petrella - nov. 1999 	*/
    /* Si se presiona ESC se borra la tabla RSacaTmp */
    IF (LASTKEY == 0x0190) THEN
	  CALL CDSexisRSacaTmp
	  if (retval >= 0)
		  let common:csr_string = "Los datos no quedarán guardados."
      	  errmsg common:errmsg_list,FLD:common:csr_string
	  end if
        call CDSborroRSacaTmp
    ELSE
        /* Si se presiona ENTER se  */
        IF (LASTKEY == 0X0191) THEN
		IF (adt_proc_tipo == "UP")
	            call CDSextUltimoRSacaTmp
	            call MISCrefRecepSaca
		END IF
        END IF
    END IF
END SUB

/*******/
