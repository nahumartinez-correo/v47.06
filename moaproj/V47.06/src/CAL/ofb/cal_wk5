/****o* Pendientes de agrupación/OFB:cal:cal_wk5
* NOMBRE
*   OFB:cal:cal_wk5
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "calofb.h"

	SUB cal:cal_wk5
	SELECT (cal:day_wk_tmp)
		CASE 1
			FPRINT cal:cal51,"%s",cal:day_tmp
			IF (cal:day_tmp == cal:days_tmp)
				LET INDcur_tag = TAG:PBcb51
			END IF
		BREAK
		CASE 2
			FPRINT cal:cal52,"%s",cal:day_tmp
			IF (cal:day_tmp == cal:days_tmp)
				LET INDcur_tag = TAG:PBcb52
			END IF
		BREAK
		CASE 3
			FPRINT cal:cal53,"%s",cal:day_tmp
			IF (cal:day_tmp == cal:days_tmp)
				LET INDcur_tag = TAG:PBcb53
			END IF
		BREAK
		CASE 4
			FPRINT cal:cal54,"%s",cal:day_tmp
			IF (cal:day_tmp == cal:days_tmp)
				LET INDcur_tag = TAG:PBcb54
			END IF
		BREAK
		CASE 5
			FPRINT cal:cal55,"%s",cal:day_tmp
			IF (cal:day_tmp == cal:days_tmp)
				LET INDcur_tag = TAG:PBcb55
			END IF
		BREAK
		CASE 6
			FPRINT cal:cal56,"%s",cal:day_tmp
			IF (cal:day_tmp == cal:days_tmp)
				LET INDcur_tag = TAG:PBcb56
			END IF
		BREAK
		CASE 7
			FPRINT cal:cal57,"%s",cal:day_tmp
			IF (cal:day_tmp == cal:days_tmp)
				LET INDcur_tag = TAG:PBcb57
			END IF
		BREAK
	END SELECT
	END SUB

/*******/
