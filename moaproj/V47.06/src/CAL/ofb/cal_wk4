/****o* Pendientes de agrupación/OFB:cal:cal_wk4
* NOMBRE
*   OFB:cal:cal_wk4
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "calofb.h"

	SUB cal:cal_wk4
	SELECT (cal:day_wk_tmp)
		CASE 1
			FPRINT cal:cal41,"%s",cal:day_tmp
			IF (cal:day_tmp == cal:days_tmp)
				LET INDcur_tag = TAG:PBcb41
			END IF
		BREAK
		CASE 2
			FPRINT cal:cal42,"%s",cal:day_tmp
			IF (cal:day_tmp == cal:days_tmp)
				LET INDcur_tag = TAG:PBcb42
			END IF
		BREAK
		CASE 3
			FPRINT cal:cal43,"%s",cal:day_tmp
			IF (cal:day_tmp == cal:days_tmp)
				LET INDcur_tag = TAG:PBcb43
			END IF
		BREAK
		CASE 4
			FPRINT cal:cal44,"%s",cal:day_tmp
			IF (cal:day_tmp == cal:days_tmp)
				LET INDcur_tag = TAG:PBcb44
			END IF
		BREAK
		CASE 5
			FPRINT cal:cal45,"%s",cal:day_tmp
			IF (cal:day_tmp == cal:days_tmp)
				LET INDcur_tag = TAG:PBcb45
			END IF
		BREAK
		CASE 6
			FPRINT cal:cal46,"%s",cal:day_tmp
			IF (cal:day_tmp == cal:days_tmp)
				LET INDcur_tag = TAG:PBcb46
			END IF
		BREAK
		CASE 7
			FPRINT cal:cal47,"%s",cal:day_tmp
			IF (cal:day_tmp == cal:days_tmp)
				LET INDcur_tag = TAG:PBcb47
			END IF
		BREAK
	END SELECT
	END SUB

/*******/
