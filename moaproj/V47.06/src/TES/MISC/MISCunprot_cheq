/****o* Pendientes de agrupación/OFB:tes:MISCunprot_cheq
* NOMBRE
*   OFB:tes:MISCunprot_cheq
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "tesMISC.h"

SUB tes:MISCunprot_cheq
    drv:button = 4
    call drv:but_enable
    drv:button = 5
    call drv:but_enable
    drv:button = 6
    call drv:but_enable
    drv:button = 7
    call drv:but_enable
    drv:button = 8
    call drv:but_enable
    drv:button = 9
    call drv:but_enable
    drv:button = 10
    call drv:but_enable    
    unprotfield siaf:nro_cheque
    unprotfield siaf:cheqcuenta
    unprotfield siaf:cod_banco
    unprotfield tes:clearing
END SUB

/*******/
