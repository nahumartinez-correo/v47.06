/****s* Pendientes de agrupación/SCR:ovr:SCRNlocal_supovr
* NOMBRE
*   SCR:ovr:SCRNlocal_supovr
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "ovrSCR.h"

SCREEN ovr:SCRNlocal_supovr,16,234,608,90,4,1,5,8,18,0
    SETFONT "Kennewick"
    SETFACE 0x0000 
    SETSIZE 16
    DRAW  TEXT 229,33,get_sup_branch
    DRAW  TEXT 320,70,get_sup_passwd
    DRAW  TEXT 60,70,get_sup_id
    SETBCOLOR 0x17 
    DRAW  FIELD 222,69,operator
    DRAW  FIELD 483,69,passwd
    DRAW  FIELD 312,32,branch
END SCREEN 

/*******/
