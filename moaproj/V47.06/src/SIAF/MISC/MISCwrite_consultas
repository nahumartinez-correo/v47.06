/****o* Pendientes de agrupación/OFB:siaf:MISCwrite_consultas
* NOMBRE
*   OFB:siaf:MISCwrite_consultas
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "siafMISC.h"

SUB siaf:MISCwrite_consultas /* creada para giros */
// Rutina genérica para imprimir una línea en la impresora
// siaf:nota_1  --->   la linea a imprimir
// FLD:giros:INDrutina ---> la rutina de encabezado de la hoja que imprime los titulos
// 		`					del listado cada vez que se acaba la hoja
	FPRINT common:csr_string,"     %s\n",siaf:nota_1
    CALL CSR:file_write 1 common:csr_string
    IF (RETVAL < 0) THEN
    	ERRMSG LST:common:errmsg_list, "Error al grabar el archivo listado.lis"
        RETURN (-2)
    END IF
    LET i = i + 1
    IF (i > 60) THEN
      	LET i = 1
		CALL FLD:giros:INDrutina      	
	END IF
END SUB

/*******/
