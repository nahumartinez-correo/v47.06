/****s* Pendientes de agrupación/SCR:siaf:SCRNlocal_supovr
* NOMBRE
*   SCR:siaf:SCRNlocal_supovr
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "siafSCR.h"

SCREEN siaf:SCRNlocal_supovr,16,300,608,90,4,1,5,8,18,0
    SETFONT "Kennewick"
    SETFACE 0x0000 
    SETSIZE 16
    DRAW  TEXT 16,54,get_sup_id
    DRAW  TEXT 322,54,ovr:get_sup_passwd
    SETBCOLOR 0x17 
    DRAW  FIELD 175,54,login
    DRAW  FIELD 471,54,ovr:passwd
END SCREEN 

/*******/
