/****o* Pendientes de agrupación/OFB:hcomm:RENmisc_set_req
* NOMBRE
*   OFB:hcomm:RENmisc_set_req
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "REENTRYMISC.OFB.h"

SUB hcomm:RENmisc_set_req

	/* No Funcionalidad de Reentry Automatico */
	/*

	if (testflag(lib:state_flags,na))
		setflag op:op_state_flags,na_reentry_req
	else
		if (testflag(lib:state_flags,teller))
			setflag op:op_state_flags,tlr_reentry_req
		else
			if (testflag(lib:state_flags,loans))
				setflag op:op_state_flags,cl_reentry_req
			END IF
		END IF
	END IF

	// EC Integ.de cambios hechos anteriormente en Version 2.7 
 	// PABLO Y MARTIN modifican el 24/08/1994 

	if (lib:stat_host_mode == dsc:hcomm:host_upmode) then
		setflag hcomm:state_flags,on_line
	else
		resetflag hcomm:state_flags,on_line
	END IF

	move "REN" to lib:stat_reentry
	if (testflag(hcomm:state_flags,on_line))
		let lib:stat_reentry.highlite = 1
	else
		let lib:stat_reentry.highlite = 0
	END IF
	refresh lib:stat_reentry

	*/
END SUB

/*******/
