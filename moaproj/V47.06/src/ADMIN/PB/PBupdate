/****o* Pendientes de agrupación/OFB:admin:PBupdate
* NOMBRE
*   OFB:admin:PBupdate
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "adminPB.h"

SUB admin:PBupdate
	/*Modificación para que en los ABM´s de tablas puedan ser consultados pero no 
	actualizados por los operadores que tengan perfil de Autoimpresión - Gabriel Falciola*/
	if (op:op_autoimpre_auth == "X" && !testflag(post:autoimpresion_flags,abm_autoimpresion))
		msgbox dsc:post:ERRperfil_no_auto,OFB_OK,"Aviso"
	else
    	select (drv:dt_app)
		case 20	     /* Perfil: Jefe de Oficina */
		case APP_TELLER  /* Perfil: Cajero */
		case APP_NA      /* Perfil: Merchandising */
			msgbox dsc:post:ERRperfil_no_auto,OFB_OK,"Aviso"
			break
		case else
			SETFLAG admin:mode_flags,update
			UNGETKEY RT_SEND
			break
		end select
	end if
END SUB

/*******/
