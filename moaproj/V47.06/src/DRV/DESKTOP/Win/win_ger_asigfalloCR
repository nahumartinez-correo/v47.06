/****o* Contra Reembolso/OFB:drv:win_ger_asigfalloCR
* NOMBRE
* OFB:drv:win_ger_asigfalloCR
* DESCRIPCION
* Llama a la transaccion de Asignacion Fallo CREM desde el menu colgante de Jefe de Oficina.
* REFERENCIADO POR
* PDM:drv:gerente
* SOURCE
*/
#include "DesktopWin.h"


SUB drv:win_ger_asigfalloCR
/*
#if CORREO_DEBUG == ON
	;if (plataf:current_state == CONFIRMATION) then
		call post:MISClogMenues("drv:win_ger_asigfalloCR")
	;endif
#endif

    if (testflag(tlr:state_flags,tran_active)) then
	    errmsg common:errmsg_list,DSC:tlr:ERRtran_act
	    return
    end if
    PDM GETSTATE gerente,22,menu_state
    IF (menu_state == MOA_CHECKED) THEN
		RETURN
    END IF

    PDM CHECKITEM gerente,22,MOA_CHECKED
    CALL do_mode
    new_app = 55

    INDrun_funct = LST:post:TRANasignFalloCREM
    post:asign_falloCR_pdm = "S"
*/
END SUB
/*******/
