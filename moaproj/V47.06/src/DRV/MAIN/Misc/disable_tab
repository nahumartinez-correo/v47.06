/****o* Pendientes de agrupación/OFB:drv:disable_tab
* NOMBRE
*   OFB:drv:disable_tab
* DESCRIPCION
*   Comportamiento del objeto (obligatorio)
* ENTRADAS
*   Parámetros de entrada de la rutina, sean argumentos o campos que tiene en cuenta
* SALIDAS
*   Campos modificados como resultado o returns cuyos valores sean no triviales
* IMPACTOS
*   Otros objetos que deberán ser analizados al modificar el objeto actual
* REFERENCIADO POR
*   Objetos que llaman al objeto documentado (obligatorio)
* DOCUMENTACION
*   Documentación relevante que deberá ser actualizada por la modificación de la rutina
* SOURCE
*/
#include "MainMisc.h"

        SUB drv:disable_tab
SELECT (drv:tab_active)
    CASE 0
        DISABLECONTROL drv:PBnb0
        BREAK

    CASE 1
        DISABLECONTROL drv:PBnb1
        BREAK

    CASE 2
        DISABLECONTROL drv:PBnb2
        BREAK

    CASE 3
        DISABLECONTROL drv:PBnb3
        BREAK

    CASE 4
        DISABLECONTROL drv:PBnb4
        BREAK

    CASE 5
        DISABLECONTROL drv:PBnb5
        BREAK

    CASE 6
        DISABLECONTROL drv:PBnb6
        BREAK

    CASE 7
        DISABLECONTROL drv:PBnb7
        BREAK

    CASE 8
        DISABLECONTROL drv:PBnb8
        BREAK

    CASE 9
        DISABLECONTROL drv:PBnb9
        BREAK

    CASE 10
        DISABLECONTROL drv:PBnb10
        BREAK

    CASE 11
        DISABLECONTROL drv:PBnb11
        BREAK

END SELECT
END SUB

/*******/
